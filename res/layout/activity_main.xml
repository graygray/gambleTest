<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.gambletest.MainActivity" >

  <!--  <Spinner
       android:id="@+id/spinner_max_round"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:entries="@array/maxRound_arrays"
       android:prompt="@string/maxRound_prompt" />
   
   <Spinner
       android:id="@+id/spinner_max_bet"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:layout_toRightOf="@+id/spinner_max_round"
       android:layout_toEndOf="@+id/spinner_max_round"
       android:entries="@array/maxBetMoney_arrays"
       android:prompt="@string/maxBetMoney_prompt" />
   
   <Spinner
       android:id="@+id/spinner_contSameToBet"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:layout_below="@+id/spinner_max_bet"
       android:entries="@array/contSame_arrays"
       android:prompt="@string/contSame_prompt" />
    -->
   <Button
       android:id="@+id/button_simulate"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:text="@string/s_simulate"
       android:layout_below="@+id/spinner_contSameToBet" />
   
   <!-- <ScrollView
        android:id="@+id/scrollView2"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/button_simulate" >

		<TextView
            android:id="@+id/tv_log"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ems="10" />
        
   </ScrollView> -->
   
   <ListView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/button_simulate"
        android:fastScrollAlwaysVisible="true"
    	android:fastScrollEnabled="true" />
        
</RelativeLayout>
