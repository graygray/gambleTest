<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

   <ListPreference
        android:defaultValue="3"
        android:entries="@array/pref_contSameToBet_titles"
        android:entryValues="@array/pref_contSameToBet_values"
        android:key="pref_contSameToBet"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_contSameToBet_messages" />

</PreferenceScreen>
